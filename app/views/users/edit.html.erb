<% provide(:title, "Edit user") %>
<h1>Update your profile</h1>

<div class="row">
  <div class="span6 offset3">
  	
  	<% # How does this form know to send a PATCH request (for editing users) 
  	   # instead of POST (for new users)? 
  	   # ANSWER: when constructing a form using form_for(@user), Rails uses POST
  	   #   if @user.new_record? is true and PATCH if it is false.
  	   #   see http://ruby.railstutorial.org/chapters/updating-showing-and-deleting-users#top
  	   #   above Listing 9.5.
  	%>
    <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages' %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :email %>
      <%= f.text_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.label :password_confirmation, "Confirm Password" %>
      <%= f.password_field :password_confirmation %>

      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>

    <%= gravatar_for @user %>
    <a href="http://gravatar.com/emails">change</a>
  </div>
</div>